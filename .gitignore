node_modules/
playwright-report/
test-results/
.env
.auth/
*.log
.DS_Store

# k6 performance test output (only produced when using --summary-export etc.)
perf/results/
*.summary.json

# Allure
allure-results/
allure-report/
# Allure history snapshots — Docker volume, run-specific, not for source control
allure-history/
